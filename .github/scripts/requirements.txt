# Dependencies for the automation scripts in this directory only.
#
# Kept separate from the repository's own requirements.txt so installing this
# template does not add entries to your project's dependency list.
#
# Upper bounds are deliberate: these scripts hold write access to your
# repository, so a major-version bump should be a decision, not a surprise.

PyGithub>=2.1.1,<3
requests>=2.31.0,<3
