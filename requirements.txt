# Your project's dependencies go here.
#
# The automation scripts' own dependencies live in
# .github/scripts/requirements.txt and are installed separately by the
# workflows, so this file stays yours.
